---
日期: 2026-06-22
文档类型：技术架构文档
文档概述: DeepSeek-Reasonix 项目的完整架构分析，涵盖技术栈、模块划分、数据流和基础设施
---

# DeepSeek-Reasonix 项目框架说明

## 一、项目概述

**DeepSeek-Reasonix** 是一个以 DeepSeek 为核心的 AI 编码代理（Coding Agent），运行在终端中。它是一个配置驱动、插件驱动的开发工具，以单一静态 Go 二进制文件交付，针对 DeepSeek 的前缀缓存（Prefix Cache）进行了优化，可在长时间编码会话中保持低 Token 成本。

> 类比：可以理解为一个开源版的 Claude Code / Cursor，但专为 DeepSeek 模型优化。

### 核心特性
- 🖥️ **多前端支持**：CLI 终端、HTTP/SSE 服务器、Wails 桌面应用、IM 机器人（QQ/飞书/微信）
- 🧠 **推理链可视化**：实时展示模型的思维过程（Chain-of-Thought）
- 🔧 **工具调用系统**：内置 bash、文件读写、grep 等工具，支持 MCP 插件扩展
- 💾 **缓存优先架构**：系统提示前缀保持字节级稳定，充分利用 DeepSeek 的自动前缀缓存
- 🌐 **多模型支持**：DeepSeek、MiMo、MiniMax、Anthropic Claude 等

---

## 二、技术栈总览

```
┌─────────────────────────────────────────────────────────────┐
│                      DeepSeek-Reasonix                       │
├─────────────────────────────────────────────────────────────┤
│  前端层                                                      │
│  ├── CLI TUI: Bubble Tea v2 + Bubbles + Lip Gloss           │
│  ├── 桌面端: Wails v2 (Go + WebKit) + React 19 + Vite 8     │
│  ├── HTTP/SSE: 内嵌 HTTP 服务器                               │
│  └── IM Bot: QQ / 飞书 / 微信 机器人网关                      │
├─────────────────────────────────────────────────────────────┤
│  控制层                                                      │
│  └── control.Controller (传输无关的统一控制器)                 │
├─────────────────────────────────────────────────────────────┤
│  代理层                                                      │
│  ├── agent: 代理循环、会话、协调器                            │
│  ├── tool/builtin: 内置工具 (bash, read_file, grep...)       │
│  ├── plugin: MCP 客户端 (stdio + HTTP, JSON-RPC)             │
│  ├── skill: 技能发现与执行                                    │
│  └── hook: Shell 钩子 (PreToolUse, PostToolUse...)           │
├─────────────────────────────────────────────────────────────┤
│  提供者层                                                    │
│  ├── provider/openai: OpenAI 兼容 (DeepSeek, MiMo, MiniMax) │
│  └── provider/anthropic: Anthropic Messages API (Claude)     │
├─────────────────────────────────────────────────────────────┤
│  基础设施层                                                  │
│  ├── config: TOML 配置加载                                    │
│  ├── store: 会话持久化 (JSONL)                                │
│  ├── memory: REASONIX.md 层级 + 自动记忆                     │
│  ├── checkpoint: 快照式回退                                    │
│  ├── i18n: 国际化 (中文/英文)                                 │
│  └── sandbox: OS 级沙箱                                       │
└─────────────────────────────────────────────────────────────┘
```

### 关键依赖

| 依赖 | 用途 |
|------|------|
| `charm.land/bubbletea/v2` | 终端 UI 框架（Elm 架构 TUI） |
| `charm.land/bubbles/v2` | TUI 组件库（文本输入、列表等） |
| `charm.land/lipgloss/v2` | 终端样式/布局 |
| `github.com/BurntSushi/toml` | TOML 配置解析 |
| `github.com/gorilla/websocket` | WebSocket 支持（MCP 插件、Bot） |
| `github.com/yuin/goldmark` | Markdown 渲染 |
| `github.com/zalando/go-keyring` | OS 凭证存储 |

---

## 三、目录结构

```
DeepSeek-Reasonix/
├── cmd/                          # 入口点
│   ├── reasonix/                 # CLI 主入口 (main.go)
│   ├── reasonix-plugin-example/  # 示例插件
│   └── e2ebench/                 # E2E 基准测试运行器
├── internal/                     # Go 内核 (43 个子包)
│   ├── agent/                    # 代理循环、会话、协调器
│   ├── cli/                      # TUI、子命令、设置向导
│   ├── control/                  # 传输无关的控制器 (核心！)
│   ├── config/                   # TOML 配置加载
│   ├── provider/                 # 模型后端抽象
│   │   ├── openai/               #   OpenAI 兼容 (DeepSeek, MiMo, MiniMax)
│   │   └── anthropic/            #   Anthropic Messages API
│   ├── tool/                     # 工具注册与执行框架
│   │   └── builtin/              #   内置工具
│   ├── plugin/                   # MCP 客户端
│   ├── serve/                    # HTTP/SSE 服务器前端
│   ├── bot/                      # IM 机器人网关
│   ├── botruntime/               # 机器人运行时支持
│   ├── event/                    # 类型化事件流
│   ├── hook/                     # Shell 钩子运行器
│   ├── memory/                   # 记忆系统
│   ├── skill/                    # 技能发现与执行
│   ├── permission/               # 工具审批模式
│   ├── sandbox/                  # OS 级沙箱
│   ├── checkpoint/               # 快照式回退
│   ├── store/                    # 会话持久化
│   ├── i18n/                     # 国际化
│   ├── diff/                     # 差异比较
│   ├── history/                  # 历史记录
│   ├── billing/                  # 计费/成本
│   ├── jobs/                     # 后台任务
│   ├── lsp/                      # LSP 集成
│   └── ...                       # 其他辅助包
├── desktop/                      # Wails 桌面应用 (独立 Go 模块)
│   ├── frontend/                 # React 19 + TypeScript 前端
│   │   └── src/
│   │       ├── App.tsx           # 单体应用壳 (~2600 行)
│   │       ├── styles.css        # 全局样式 (25,648 行!)
│   │       ├── components/       # ~55 个 UI 组件
│   │       ├── lib/              # ~50 个工具/hook 文件
│   │       ├── custom/           # 插件式功能扩展
│   │       └── locales/          # i18n 字典
│   ├── app.go                    # Wails 应用绑定
│   ├── tabs.go                   # 标签页管理
│   └── wails.json                # Wails 配置
├── site/                         # Astro 文档网站
├── npm/                          # npm 包构建脚本
├── workers/                      # Cloudflare Workers
│   └── crash-report/             # 崩溃报告服务
├── benchmarks/                   # E2E 和上下文维护基准测试
├── scripts/                      # 发布/构建/CI 辅助脚本
├── docs/                         # 工程规范、指南、迁移文档
├── .github/                      # CI 工作流、Issue 模板
├── Makefile                      # 构建命令
├── reasonix.example.toml         # 配置示例
├── REASONIX.md                   # 项目指令（加载到系统提示）
├── go.mod                        # Go 模块定义
└── .goreleaser.yaml              # 发布配置
```

---

## 四、核心架构设计

### 4.1 传输无关的控制器（Controller）

这是整个项目最核心的设计。`control.Controller` 是所有行为的唯一入口：

```
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│  CLI TUI │  │ HTTP/SSE │  │  Desktop │  │  IM Bot  │
└────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘
     │             │             │             │
     └─────────────┴──────┬──────┴─────────────┘
                          │
                 ┌────────▼────────┐
                 │   Controller    │  ← 所有逻辑在此
                 └────────┬────────┘
                          │
           ┌──────────────┼──────────────┐
           │              │              │
     ┌─────▼─────┐ ┌─────▼─────┐ ┌─────▼─────┐
     │   Agent   │ │   Tools   │ │  Provider  │
     └───────────┘ └───────────┘ └───────────┘
```

**优势**：无论用户通过 CLI、桌面端还是机器人使用，行为完全一致，无逻辑重复。

### 4.2 缓存优先架构

```
┌─────────────────────────────────────────────┐
│              系统提示前缀 (稳定)              │
│  ┌─────────┐ ┌──────┐ ┌────────┐           │
│  │基础提示  │ │工具集 │ │记忆文件 │           │
│  └─────────┘ └──────┘ └────────┘           │
├─────────────────────────────────────────────┤ ← 缓存边界
│              对话尾部 (变化)                  │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐     │
│  │历史消息  │ │推理语言   │ │用户输入   │     │
│  └─────────┘ └──────────┘ └──────────┘     │
└─────────────────────────────────────────────┘
```

系统提示前缀在整个会话中保持字节级稳定，使 DeepSeek 的自动前缀缓存持续命中。

### 4.3 代理循环（Agent Loop）

```
用户输入 → 输入组合 → Provider 流式调用 → 收集 Chunk
    │                                        │
    │         ┌──────────────────────────────┘
    │         ▼
    │    ┌─────────┐     ┌─────────┐
    │    │文本 Chunk│     │工具调用  │
    │    └────┬────┘     └────┬────┘
    │         │               │
    │         ▼               ▼
    │    流式显示文本     executeBatch()
    │                    ┌────┴────┐
    │                    │只读工具  │写入工具│
    │                    │(并行×8) │(串行) │
    │                    └────┬────┘
    │                         │
    │         ┌───────────────┘
    │         ▼
    │    附加工具结果 → 继续循环
    │
    └── 无工具调用 + 有文本 → 会话结束
```

### 4.4 事件流系统

```
Agent 事件 → event.Event → event.Sink → Broadcaster → SSE 客户端
                                                  → TUI 渲染
                                                  → Wails 前端
```

事件类型包括：`turn_started`, `text`, `reasoning`, `tool_dispatch`, `tool_result`, `usage`, `approval_request`, `turn_done`, `compaction_started` 等。

---

## 五、AI/推理特性

### 5.1 多协议推理支持

| 协议 | 模型 | 推理级别 |
|------|------|----------|
| DeepSeek | deepseek-v4-flash, deepseek-v4-pro | high, max |
| OpenAI | MiMo 等 | low, medium, high |
| Anthropic | Claude | low, medium, high, xhigh, max |
| MiniMax | MiniMax-M3 | adaptive, disabled |

### 5.2 推理流式处理

```go
// Chunk 类型
ChunkText          // 可见文本增量
ChunkReasoning     // 思维链增量
ChunkToolCallStart // 工具调用开始
ChunkToolCall      // 完整工具调用
ChunkUsage         // Token 使用量
ChunkDone          // 完成
ChunkError         // 错误
```

### 5.3 推理语言偏好

用户可以控制推理文本的显示语言（中文/英文），通过 `<reasoning-language>` 标签注入到对话尾部（非系统前缀），保持缓存稳定。

### 5.4 双模型协调

```
┌─────────────┐     ┌─────────────┐
│ 规划模型     │────▶│ 执行模型     │
│ (只读分析)   │     │ (工具调用)   │
└─────────────┘     └─────────────┘
```

---

## 六、前端架构（桌面端）

### 6.1 组件层级

```
StrictMode
  ErrorBoundary
    LocaleProvider          (i18n)
      ToastProvider         (通知)
        App                 (单体应用壳)
          AppChrome          ← 标题栏 + 标签页
          aside.sidebar      ← 项目树
          main               ← 中央聊天区
            Transcript       ← 消息列表（虚拟化）
            Composer         ← 输入区（斜杠命令、附件、模型切换）
            StatusBar        ← 底部状态栏
          aside.right-dock   ← 上下文面板、工作区面板
          SettingsPanel      ← 14 标签设置弹窗
          CommandPalette     ← 命令面板 (Cmd+K)
          ApprovalModal      ← 工具审批对话框
```

### 6.2 状态管理

**核心：`useController` hook** — 基于 reducer 的自定义架构，无外部状态库

- 维护 `Map<string, State>` 按标签页隔离状态
- 17 种 action 类型处理后端事件
- 使用 `requestAnimationFrame` 批处理高频 token 事件

### 6.3 前后端通信

```
React 前端 ←→ bridge.ts ←→ Wails 绑定 ←→ Go 后端
```

`bridge.ts` 提供：
- `app` 对象：Proxy 转发到 `window.go.main.App`（130+ 绑定方法）
- `onEvent(cb)`：订阅后端事件流
- 浏览器开发模拟（`makeMockApp()`）：独立于 Go 后端开发

---

## 七、HTTP API 端点

### 只读端点 (GET)

| 端点 | 用途 |
|------|------|
| `GET /` | 嵌入式 HTML 页面 |
| `GET /events` | SSE 事件流 |
| `GET /history` | 完整会话历史 (JSON) |
| `GET /context` | Token 使用量/上下文窗口 |
| `GET /checkpoints` | 回退检查点列表 |
| `GET /status` | 综合状态 |
| `GET /sessions` | 已保存会话列表 |
| `GET /skills` | 可发现技能列表 |

### 操作端点 (POST)

| 端点 | 用途 |
|------|------|
| `POST /submit` | 提交用户输入 |
| `POST /cancel` | 取消运行中的回合 |
| `POST /approve` | 批准/拒绝工具调用 |
| `POST /answer` | 回答 ask 工具问题 |
| `POST /plan` | 切换计划模式 |
| `POST /compact` | 强制上下文压缩 |
| `POST /new` | 新建会话 |
| `POST /rewind` | 回退到检查点 |

---

## 八、数据存储

### 8.1 会话数据

- **格式**：JSONL 文件（无传统数据库）
- **路径**：由 `internal/store/session.go` 管理
- **内容**：消息历史、检查点、目标、任务产物

### 8.2 配置管理

```
解析优先级: 命令行标志 > ./reasonix.toml > ~/.config/reasonix/config.toml > 内置默认值
```

配置文件 `reasonix.toml` 涵盖：
- 默认模型、UI 主题、代理参数
- 提供者配置（API Key 通过环境变量引用）
- 工具、技能、插件、机器人网关配置

---

## 九、基础设施与部署

### 9.1 构建系统

| 工具 | 用途 |
|------|------|
| Makefile | 本地构建 (`make build`, `make test`, `make cross`) |
| GoReleaser | CLI 二进制发布（6 平台交叉编译） |
| Wails CLI | 桌面应用构建 |
| npm build.mjs | npm 包分发（平台特定子包） |

### 9.2 CI/CD (GitHub Actions)

| 工作流 | 触发条件 | 功能 |
|--------|----------|------|
| ci.yml | push/PR to main-v2 | 测试、lint、覆盖率、竞态检测 |
| release.yml | tag v* | CLI 二进制发布 + Homebrew |
| release-npm.yml | tag npm-v* | npm 包发布 |
| release-desktop.yml | tag desktop-v* | 桌面应用发布（macOS 签名公证） |
| pages.yml | site/ 变更 | 文档网站部署 |
| deploy-crash-worker.yml | workers/ 变更 | Cloudflare Worker 部署 |
| codeql.yml | push/PR + 周一 | 安全扫描 |

### 9.3 外部服务

| 服务 | 技术 | 用途 |
|------|------|------|
| crash.reasonix.io | Cloudflare Worker + D1 | 崩溃报告、DAU 统计 |
| dl.reasonix.io | Cloudflare R2 | 桌面更新 CDN |
| reasonix.io | GitHub Pages + Astro | 文档网站 |
| npm registry | npm | 包分发 |
| Homebrew | 自定义 Tap | macOS 安装 |

### 9.4 测试策略

- **单元测试**：345 个 `*_test.go` 文件
- **竞态检测**：CI 专用 `-race` 任务
- **E2E 基准**：5 种任务类型（代码生成、Bug 修复、上下文维护等）
- **缓存守卫**：验证前缀缓存命中率稳定性
- **安全扫描**：CodeQL + govulncheck

---

## 十、关键设计原则总结

| 原则 | 说明 |
|------|------|
| **缓存优先** | 系统提示前缀字节级稳定，充分利用 DeepSeek 前缀缓存 |
| **传输无关** | Controller 是唯一行为入口，前端只是视图 |
| **配置驱动** | 提供者、工具、插件、技能均通过 TOML 声明 |
| **单一静态二进制** | `CGO_ENABLED=0`，无运行时依赖 |
| **接口隔离** | Controller 拆分为 10 个子接口，各前端按需依赖 |
| **事件驱动** | 类型化事件流解耦代理与前端 |
| **自注册** | 工具和提供者通过 `init()` 自动注册 |
